Absolutely — here is a cleaned-up version with the spelling, grammar, and wording fixed, while keeping your meaning the same and correcting the text-to-speech typos like “death journal” to “dev journal” and “main” where appropriate. I also smoothed out a few places where the original wording was awkward or unclear.
What I did
I realized that Homebrew was needed, so I installed it, added it to my PATH, installed FNM with Homebrew, and set Node LTS as the default. I then generated an SSH key, copied it into GitHub, and tested the connection successfully. After that, I created the GitHub repository called “dev journal” and added a README file. I also installed VS Code and the extensions ESLint, Prettier, GitLens, and Error Lens, and set Prettier as the default formatter so my code would be automatically formatted when I saved files. I then created the journal repository file, edited it in VS Code, and got it ready to commit and push.
What confused me
What confused me was that I did not fully understand what Homebrew was at first, even though I needed it. I also moved too quickly instead of reading each output carefully before running the next command, and I was copying Claude’s instructions without really understanding them. While generating the SSH key, I got confused about the save prompt and should have just pressed Enter instead of typing a name. I was also missing the repository setup at first, which I later fixed, and the final step still involved committing and pushing the new journal file to GitHub.
Definitions I learned today
Commit: taking a snapshot of your changes.
Main: the official version of the code lives here.
Branch: a copied version of the main branch where you can make edits without disturbing the main version.
Merge: combining your branch back into main when you are confident with your changes.
Pull request: a proposal to merge a branch into main, where others review the changes before they are published.
Flow of doing things on GitHub
Git clone > git checkout > git add > git commit > git push > pull request on GitHub.com.
Questions for next session
Why do we have different tools to install different things, like why did I need Homebrew and why did I need FNM? I also have experience with command-line tools in Linux and PowerShell, and I have learned some Bash commands. I also did some Bash programming, so how can that help me, and how can I incorporate it into my learning? I also need a retention technique because I often forget things, so I need better ways to learn and retain information. What should I repeat again and again so I do not forget it?
Tool relationship
Homebrew installs system tools, including FNM. FNM installs Node and lets you switch versions. Node comes with npm, and npm installs JavaScript libraries inside specific projects.
A small note: in your original text, “main” was sometimes typed as “man,” “men,” or “Maine,” and “merge” was sometimes typed as “march,” so I corrected those to the intended Git terms.
