what i did?
I realized that Homebrew was needed, so I installed it, added it to my PATH, installed FNM with Homebrew, and set up Node LTS as the default. I then generated an SSH key, copied it into GitHub, and tested the connection successfully. After that, I created the GitHub repository called “death journal” and added a README file. I also installed VS Code and the extensions ESLint, Prettier, GitLens, and Error Lens, and set Prettier as the default formatter so my code would be automatically formatted when I saved files. I then created the journal repository file, edited it in VS Code, and got it ready to commit and push.

what confused me?
Where I lagged behind or got confused was that I did not fully understand what Homebrew was at first, even though I needed it. I also moved too quickly instead of reading each output carefully before running the next command, and I was copying Claude’s instructions without really understanding them. While generating the SSH key, I got confused about the save prompt and should have just pressed Enter instead of typing a name. I was also missing the repository setup at first, which I later fixed, and the final step still involved committing and pushing the new journal file to GitHub.

Definitions i learned today:
Commit: it's like taking a snapshot of your changes
Main: this is where the official version of the code lives
Branch: this is a copied version of the main where you can make edits without disturbing the man
Merge: we use this to merge our branches into men when we are confident with our changes.
Pull request: it is a proposal to march branch into Maine. This is where everyone reviews the changes and agrees to publish it.
Flow of doing things on github.
Git clone> git checkout > git add > git commit > git push > pull request on github.com

Questions for next session:
Why do we have different libraries to install different things like why did I need home brew and why did I need FNM? Also I do have a experience in command line tools with Lennox and power shell I did learn some bash commands. I do remember I also did some bash programming. My question here is how can that help me and if there is a way to incorporate that in my learning? Also, I need a retention technique because I often forget things since I'm 27 years old now and I need better ways of learning and retaining things so please tell me which things I need to like fix in my memory that I need to repeat again and again, so I don't forget it?
